## 改动说明

说明本 PR 解决的问题、主要实现和不在范围内的内容。

## 行为变化

- 用户可见变化：
- 默认设置变化：
- 老用户迁移：
- EA 请求变化：
- 服务端或 EXE 兼容要求：

## 验证

列出执行过的命令和实机场景：

```text
npm run check
python -m pytest backend/tests -q
```

## 风险与回滚

说明写操作、球员保护、缓存、限流、存储或页面 Hook 风险，以及出现问题时如何回滚。

## 清单

- [ ] 改动范围单一，没有混入无关格式化。
- [ ] 新行为有测试或说明无法自动测试的原因。
- [ ] 没有新增 `@ts-nocheck`、无理由 `any` 或危险全局 Hook。
- [ ] 写请求失败会核验状态，不会盲目重放。
- [ ] 三道球员保护未被削弱。
- [ ] 老用户设置不会被无意覆盖。
- [ ] 没有提交 Token、Cookie、日志、数据库、EXE 或用户数据。
- [ ] 没有引入未经授权的第三方代码。
- [ ] 文档和 `CHANGELOG.md` 已更新。
- [ ] `npm run check` 已通过。
- [ ] 后端改动的 Pytest 已通过。
